/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        unoptimized: true,
        domains: ['aceternity.com'],
    },
    optimizeFonts: false,
};


export default nextConfig;
